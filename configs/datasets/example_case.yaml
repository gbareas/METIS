# Example config — points at the synthetic mock case generated by
# scripts/generate_mock_dns.py (writes data/mock/case_mock.h5 plus a
# companion data/mock/metadata.json). See configs/datasets/case01.yaml for
# an example pointed at real DNS output.
case_id: case_mock
source: data/mock/case_mock.h5
metadata: data/mock/metadata.json
variables: [u, v, w, T, rho]
task: regime_discovery
